import pullupProgression from './progressions/progression-pullup';
import squatProgression from './progressions/progression-squat';

const firstPair = {
  pullupProgression,
  squatProgression,
};

export default firstPair;
