import rowProgression from './progressions/progression-row';
import pushupProgression from './progressions/progression-pushup';

const thirdPair = {
  rowProgression,
  pushupProgression,
};

export default thirdPair;
